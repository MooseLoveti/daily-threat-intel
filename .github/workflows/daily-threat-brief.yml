name: Daily intelligence briefs

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * *"
      timezone: "Asia/Tokyo"

# This uses the repository owner's Copilot entitlement in a personally-owned
# repository. No OpenAI API key or long-lived personal access token is stored.
permissions:
  contents: write
  copilot-requests: write

concurrency:
  group: daily-threat-brief
  cancel-in-progress: false

jobs:
  build-briefs:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out private vault repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install feed-processing dependencies
        run: python -m pip install --disable-pip-version-check -r requirements.txt

      - name: Set up Copilot CLI
        uses: actions/setup-node@v7
        with:
          node-version: "22"

      - name: Install Copilot CLI
        run: npm install --global @github/copilot

      - name: Collect threat-intelligence candidates
        run: |
          mkdir -p .runtime
          python scripts/daily_brief.py collect \
            --config config/sources.yaml \
            --state state/brief_state.json \
            --output .runtime/threat-candidates.json

      - name: Generate threat-intelligence brief with Copilot
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          candidate_count=$(python -c "import json; print(len(json.load(open('.runtime/threat-candidates.json', encoding='utf-8'))['articles']))")
          if [ "$candidate_count" -eq 0 ]; then
            python scripts/daily_brief.py empty-response --output .runtime/threat-model-output.json
            exit 0
          fi
          python scripts/daily_brief.py prompt \
            --config config/sources.yaml \
            --candidates .runtime/threat-candidates.json \
            --output .runtime/threat-prompt.txt
          # Copilot gets only the bounded prompt as input. No shell, network, or
          # file-writing tools are granted to it.
          copilot -p "$(cat .runtime/threat-prompt.txt)" --no-ask-user > .runtime/threat-model-output.json

      - name: Render threat-intelligence Markdown
        run: |
          date_jst=$(TZ=Asia/Tokyo date +%F)
          python scripts/daily_brief.py render \
            --config config/sources.yaml \
            --state state/brief_state.json \
            --candidates .runtime/threat-candidates.json \
            --model-output .runtime/threat-model-output.json \
            --output "content/daily/${date_jst}.md"

      - name: Collect AI-news candidates
        run: |
          python scripts/daily_brief.py collect \
            --config config/ai_sources.yaml \
            --state state/ai_brief_state.json \
            --output .runtime/ai-candidates.json

      - name: Generate AI-news brief with Copilot
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          candidate_count=$(python -c "import json; print(len(json.load(open('.runtime/ai-candidates.json', encoding='utf-8'))['articles']))")
          if [ "$candidate_count" -eq 0 ]; then
            python scripts/daily_brief.py empty-response --output .runtime/ai-model-output.json
            exit 0
          fi
          python scripts/daily_brief.py prompt \
            --config config/ai_sources.yaml \
            --candidates .runtime/ai-candidates.json \
            --output .runtime/ai-prompt.txt
          copilot -p "$(cat .runtime/ai-prompt.txt)" --no-ask-user > .runtime/ai-model-output.json

      - name: Render AI-news Markdown
        run: |
          date_jst=$(TZ=Asia/Tokyo date +%F)
          python scripts/daily_brief.py render \
            --config config/ai_sources.yaml \
            --state state/ai_brief_state.json \
            --candidates .runtime/ai-candidates.json \
            --model-output .runtime/ai-model-output.json \
            --output "content/ai-daily/${date_jst}.md"

      - name: Commit the daily note
        run: |
          git add content/daily content/ai-daily state
          if git diff --cached --quiet; then
            echo "No content changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "docs: add daily intelligence briefs"
          git push
